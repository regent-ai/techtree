{
  "name": "@techtree/services",
  "private": true,
  "type": "module",
  "workspaces": [
    "siwa-sidecar",
    "xmtp-worker"
  ],
  "scripts": {
    "dev": "cd siwa-sidecar && bun run dev",
    "dev:siwa": "cd siwa-sidecar && bun run dev",
    "dev:xmtp": "cd xmtp-worker && bun run dev",
    "build": "cd siwa-sidecar && bun run build && cd ../xmtp-worker && bun run build",
    "typecheck": "cd siwa-sidecar && bun run typecheck && cd ../xmtp-worker && bun run typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.0"
  }
}
